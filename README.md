# ProjectCG2526
Geração de Dados Sintéticos em Unreal e Testes com Modelos de Deep Learning.
---
A few important changes that were forgotten to mention:
- epochs is 100 instead of 50
- The idea of model.val(data="dataset.yaml") is instead model.val(data="dataset.yaml",split="test")
